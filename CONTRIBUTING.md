# Contributing

1. Fork the repository and create a branch from `main`.
2. Install dependencies: `npm ci`.
3. Run `npm run lint` and `npm test` before opening a pull request.
4. For UI or extension behavior changes, describe the scenario you tested (GitHub vs registry, sync, browse).

Pull requests should stay focused: one logical change per PR is easier to review.
